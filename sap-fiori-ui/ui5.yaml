specVersion: "2.6"
type: application
metadata:
  name: sap-fiori-ui
server:
  customMiddleware:
    - name: "ui5-middleware-simpleproxy"
      afterMiddleware: compression
      mountPath: "/odata"
      configuration:
        baseUri: "http://127.0.0.1:4004/odata"
